[package]
name = "octofs"
version = "0.7.0"
edition = "2021"
authors = ["Muvon Un Limited <contact@muvon.io>"]
license = "Apache-2.0"
description = "Standalone MCP filesystem tools server — view, edit, shell, workdir"
homepage = "https://github.com/muvon/octofs"
repository = "https://github.com/muvon/octofs"
documentation = "https://github.com/muvon/octofs"
keywords = ["mcp", "ai", "filesystem", "cli", "tools"]
categories = ["development-tools", "command-line-utilities", "filesystem"]
readme = "README.md"
rust-version = "1.95"
exclude = [
    "tests/fixtures/*",
    "examples/*",
    ".github/*",
    "docs/*",
    "scripts/*",
    "*.md",
    "install.sh"
]

[features]
remote = ["dep:russh", "dep:russh-sftp"]
[[bin]]
name = "octofs"
path = "src/main.rs"

# Enable static linking
[profile.release]
lto = true              # Enable Link Time Optimization
codegen-units = 1       # Reduce parallel code generation units for better optimization
panic = "abort"         # Abort on panic for smaller binary size
strip = true            # Strip symbols from binary
opt-level = "s"         # Optimize for size, but keep loop vectorization ("z" disables it)
overflow-checks = false # Disable overflow checks in release mode

[dependencies]
tokio = { version = "1.52.3", features = ["rt-multi-thread", "macros", "process", "fs", "sync", "signal", "io-std", "io-util", "net"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
anyhow = "1.0.102"
ignore = "0.4.25"
regex = "1.12.3"
rayon = "1.12.0"
tracing = { version = "0.1", features = ["std"] }
clap = { version = "4.6.1", features = ["derive"] }

# MCP SDK
rmcp = { version = "3.0.0", features = ["server", "macros", "transport-io", "transport-streamable-http-server"] }
axum = { version = "0.8", default-features = false, features = ["http1", "tokio"] }
tokio-util = { version = "0.7", features = ["codec"] }

# Remote filesystem support (SSH/SFTP).
# default-features = false drops russh's `rsa` feature: the `rsa` crate has no
# release with the RUSTSEC-2023-0071 (Marvin) fix, so RSA keys are unsupported —
# use ed25519/ecdsa keys and host keys instead.
russh = { version = "0.62", optional = true, default-features = false, features = ["flate2", "aws-lc-rs"] }
russh-sftp = { version = "2.3", optional = true }
[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3.27.0"
serial_test = "3.4.0"

[profile.dev]
opt-level = 1
debug = true
debug-assertions = true
overflow-checks = true
lto = false
codegen-units = 256
incremental = true
rpath = false
